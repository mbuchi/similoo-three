// Signal-collection proxy — the suite-wide Vercel edge handler.
// Re-exported from @swissnovo/shared so every app shares one implementation.
export { config, default } from '@swissnovo/shared/signal-collect';
